---
name: xiaolong-card
description: 通过小笼 AI 连接器在飞书群里发起投票、报名统计和自定义表单收集。当用户想要投票、统计报名、收集名单、发问卷表单、统计是否参加、收集 100 人名单报名情况时使用此技能。关键词：投票、报名、表单、名单、统计、收集、问卷、接龙、参加、统计人数、小队、伙伴、组队、分工、接力、协作、收集专员、播报。
---

# 小笼卡片技能

帮用户在飞书群里发起交互卡片（投票 / 报名 / 表单），成员在群里点击即投、原地统计，全程无需离开飞书。本技能可单伙伴独立使用，也可在「工作伙伴·小队」中作为收集/播报角色的能力底座。

## 前置条件

小笼 AI 连接器（`xiaolong-doubao-work`）已配置并选中。调用工具前不需要用户提「小笼」——只要意图是投票/报名/收集，就用下面的工具。

## 工具选择

| 用户意图 | 工具 | 说明 |
|---|---|---|
| 投票（选择题，2-8 个选项） | `create_vote` | 如「下午茶喝什么：奶茶/咖啡/果茶」 |
| 给一份名单统计报名 | `create_roster_form` | 名单 ≤100 人，自动生成「姓名下拉 + 能否参加」 |
| 其它收集（多个自定义字段） | `create_form` | ≤6 个字段，text/select 混合 |
| 查进度 | `get_session_status` | 需要创建时返回的 session_id |
| 导出结果 | `export_session_summary` | 文本汇总，含每人明细 |

创建类工具都会把卡片发进飞书会话并返回 `session_id`——**务必把 session_id 记录在回复里**，用户后续问「报名情况如何」时要靠它查询。

## 参数要点

- `chat_id` 不填 = 发到默认会话；用户明确指定其他群时才传。
- `create_roster_form` 的名单直接用用户提供的数组，**不要自己缩水**；超过 100 人时报错，如实告知并建议拆两批。
- `create_form` 的 select 字段 `options` ≤100 个；text 字段不要传 options。
- 选项/字段保持用户原文的措辞与顺序，不要润色。

## 伙伴协作模式

本技能既支持单个伙伴独立完成，也支持小队组队接力。**判断标准：有没有明确的交接对象。**

### 单伙伴独立完成

用户直接提需求，当前伙伴从头到尾负责：建卡 → 回复 session_id → 被追问时查进度 → 要明细时导出。不需要把中间状态交给别的伙伴。

### 小队组队接力（一棒交给下一棒）

小队拆解任务后，上一棒产出交给下一棒。**交接的唯一凭证是 `session_id`**，必须保证它在棒与棒之间不断链：

1. **建卡棒**（通常由收集专员担任）：调创建工具拿到 `session_id` 后，回复里必须写明「卡片已发到群里，session_id 是 xxx，后续查进度/导出都报这个 id」。
2. **接棒时**：如果用户或上一棒只给了描述（如「那张报名卡统计到哪了」）而没有 `session_id`，先在对话上下文里找；找不到就明确告诉对方「需要 session_id 才能查，请把建卡时返回的 id 发我」，不要凭空猜一个。
3. **收尾棒**（通常由播报伙伴担任）：拿 `session_id` 调 `get_session_status` 汇报进度、调 `export_session_summary` 导出明细，把结果整理成表格/文档交给用户。

### 角色分工速查（配合 agents/ 目录的伙伴卡）

| 角色 | 用哪些工具 | 交接物 |
|---|---|---|
| 收集统计专员（建卡） | `create_vote` / `create_roster_form` / `create_form` | `session_id` |
| 进度播报伙伴（跟进） | `get_session_status` / `export_session_summary` | 进度播报 / 导出明细 |

## 示例话术

**投票**：「帮我发起个投票，问题：周五团建去哪，选项：骑行 / 露营 / 采摘」→ 调 `create_vote`。

**100 人报名**（本技能的招牌场景）：用户给出一份 100 人名单并要求统计报名 → 调 `create_roster_form`，`names` 传完整数组：

```json
{
  "title": "全员大会报名",
  "names": ["张三", "李四", "王五", "...（共 100 个）"]
}
```

**混合表单**：「收集团建信息：姓名（必填）、忌口（选填）、交通方式（下拉：大巴/自驾/地铁）」→ 调 `create_form`。

**小队接力**：「名单伙伴先生成 100 人名单，交给收集专员建报名卡，再让播报伙伴每小时汇报一次进度」→ 建卡棒用 `create_roster_form` 并回传 `session_id`；播报棒用 `get_session_status` 周期性汇报。

## 查询与导出

用户问「报名情况」「统计到哪了」时，先从对话上文找回 session_id，再调 `get_session_status`；用户要结果明细时调 `export_session_summary`，把返回的 text 原样展示或继续加工成表格/文档。

更多示例与故障排查见 [references/examples.md](references/examples.md)。
